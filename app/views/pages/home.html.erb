<%# HTML correspondiente al Home ENG %>
<title>Paszkowicz-Group</title>

<%# division correspondiente a home ENG %>
<div class="home-container">

  <%# barra de navegacion eng%>
  <%= render 'layouts/navbar' %>

  <%# division de la presentacion %>
  <div class="home-presentation">

  </div>

  <%# division correspondiente a la presentacion de los proyectos en ingles %>
  <div class="presentation-card">
      <h1>Front-End Web Developer</h1>
      <p>As front-end web developers with advanced
      training from Le Wagon in Lille, France, we
      specialize in crafting visually appealing and
      highly interactive user interfaces. We
      prioritize responsiveness and smooth user
      experiences across all devices. With expertise
      in HTML, CSS, and JavaScript, we use modern
      libraries and frameworks to develop efficient,
      modular code that enhances website functionality
      and aesthetics.</p>

<p>Our approach combines technical proficiency with a keen eye for design, allowing us to create optimized, cross-browser compatible solutions that captivate and engage users.</p>

  </div>

  <%# carrousel de habilidades %>
  <%= render 'layouts/carrousel' %>

  <%# javascript correspondiente a la transicion de la home-presentation %>
 <script>

    let lastScrollTop = 0;
    const homePresentation = document.querySelector('.home-presentation');

    window.addEventListener('scroll', function() {
      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;

      if (scrollTop > lastScrollTop) {
        // Scrolling down
        homePresentation.classList.add('hidden');
      } else {
        // Scrolling up
        homePresentation.classList.remove('hidden');
      }
      lastScrollTop = scrollTop <= 0 ? 0 : scrollTop; // For Mobile or negative scrolling
    });

  </script>





</div>
