<%# HTML correspondiente a about en ingles %>

<%# division contenedora %>
<div class="about-card">
  <div class="presentation-card-title">
    <h1>Front-End Web Developer</h1>
  </div>

  <div class="presentation-card-text-container">

    <div class="presentation-card-text-container-a">
      <p>As front-end web developers with advanced
      training from Le Wagon in Lille, France, we
      specialize in crafting visually appealing and
      highly interactive user interfaces. We
      prioritize responsiveness and smooth user
      experiences across all devices. With expertise
      in HTML, CSS, and JavaScript, we use modern
      libraries and frameworks to develop efficient,
      modular code that enhances website functionality
      and aesthetics.</p>
    </div>

    <div class="presentation-card-text-container-b">
      <p>Our approach combines technical proficiency
      with a keen eye for design, allowing us to create
      optimized, cross-browser compatible solutions that
      captivate and engage users.</p>
    </div>

  </div>

  <%# division correspondiente a proyectos en ingles %>
  <%= render 'pages/aboutme' %>

</div>

<%# javascript correspondiente a la aparicion de la presentation-card %>
<script>

  document.addEventListener("DOMContentLoaded", () => {
    const projectPresentation = document.querySelector(".project-presentation");

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            projectPresentation.classList.add("visible");
          } else {
            projectPresentation.classList.remove("visible");
          }
        });
      },
      { threshold: 0.5 } // Adjusts how much of the element should be visible to trigger
    );

    observer.observe(projectPresentation);
  });

</script>
